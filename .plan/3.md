# Plan — #3 first-class High/Medium/Low ROADMAP layout for operator projects (M7.1.F68 option B)

- **Status:** ready (approved — product lead, batch approval via AskUserQuestion, 2026-07-07)
- **Type / priority / estimate:** design-spike (`docs`) · P2 · ~3h for the decision write-up (implementation, if Option B is chosen, is a *separate* task and is sized below, not by this estimate)
- **blocked_by:** none

> This is an **Idea / product-design** item that explicitly proposes changing a `✅ agreed` spec (PLAN.md §5). The deliverable of this task is a **documented decision** (A vs B, recorded in PLAN.md §5 / §16), *not* an implementation. The "Approach" and "Affected areas" below describe the shape Option B would take **if approved**, so the product lead can weigh it — they are not a mandate to write code. If the decision is "stay with Option A", this task closes with a PLAN.md note and no code change.

## Context recap (grounded in the code)

Today the autonomous flow (`task-orchestrator` / `/atelier:next-task`) only claims tasks from a **PLAN.md §5** roadmap: `P0`/`P1`/`P2` sections, backtick `type` tags, `` `#id` ``, optional `` `~estimate` ``, and the `[ready]` planning-gate token. That contract is defined in:

- `PLAN.md` §5 (lines 261–338) — the `✅ agreed` format, incl. the item-line grammar (`- [ ] [ready] <type> <title> <#id> <~estimate> <blocked_by:…>`, line 286) and the **Planning gate** (M4.30, lines 317–324). Line 324 states the gate governs the operator-facing P0/P1/P2 flow and does **not** apply to atelier's own `## High/Medium/Low` dev roadmap.
- `skills/task-discovery/SKILL.md` — the parser/selector. It already contains a **partial** version of what Option B asks for: the *Edge cases* section (line 117) maps `## High → P0`, `Medium → P1`, `Low → P2` for selection, and lines 118–119 define fallbacks for a missing `#id` (synthetic slug id, `id-synthesized: true`) and missing estimate. But this path is scoped to **atelier's own maintainer-run roadmap** and, critically, lines 81/86–91 **exclude the `[ready]` gate** from it ("treat every unchecked item as selectable").
- `commands/next-task.md` (step 2) and `commands/plan-task.md` — the consumers: `/next-task` reads the backlog and applies the gate; `/plan-task` flips `[ready]` (or the `github-project` `Ready` field) and commits `.plan/<id>.md`.

Real teams' roadmaps (what `/adopt-roadmap`'s default and `claude-roadmap-tools` produce) use the simpler `## High/Medium/Low` + `- [ ]` checkbox layout with no type/estimate/`#id`/`[ready]`.

- **Option A (shipped, crt #15):** convert the simple layout **to** §5 at onboarding via `/adopt-roadmap --format atelier`. atelier's `/setup-project` only *detects* the non-§5 case and *delegates* to crt (`atelier-setup-project`'s `detect_roadmap_format()` → the `atelier-roadmap-format=` signal; regression-guarded by `hooks/tests/setup-project-roadmap-format-f74.test.sh`). Format transformation lives in **crt, not atelier** — the sovereignty boundary set in `PLAN.md` line 112.
- **Option B (this task):** make `task-discovery` accept `High/Med/Low` **natively for operator projects**, apply the `[ready]` gate there, and treat `type`/`estimate` as optional — no §5 rewrite at onboarding.

## Approach (the design investigation this task delivers)

Produce a decision record that answers four questions, then either update PLAN.md §5 to bless Option B (and open a follow-up implementation task) or record "A stands, revisit later".

1. **What does §5 metadata actually buy?** Enumerate each §5-only field and its consumer, so we can judge whether Option B's "optional type/estimate" degrades anything:
   - `priority` (P0/P1/P2) — load-bearing for selection order. `High/Med/Low` already maps cleanly (task-discovery line 117), so **no loss**.
   - `#id` — load-bearing: it names the `.plan/<id>.md` artifact and the `task/<id>-<slug>` branch, and it is the target of `blocked_by`. The simple layout has none; task-discovery's synthetic-slug fallback (line 118) yields **unstable** ids (rename the title → new id → orphaned plan file / broken `blocked_by`). This is the sharpest cost of Option B and the crux of the decision.
   - `type` — used for branch-prefix hints and PR/commit conventions; degrades gracefully to a default (`feat`) if absent.
   - `~estimate` — only feeds the oversize-likely heuristic in the `planner`; absence just means the planner leans on bullet-count / keyword / dir-count heuristics instead. Tolerable.
   - `[ready]` + committed `.plan/<id>.md` — the planning gate. **This is the hard part of Option B**: the simple layout has no slot for `[ready]`, and without a stable `#id` the `.plan/<id>.md` linkage is fragile.
2. **Decide where the `[ready]` token lives in a `High/Med/Low` line.** Options to evaluate: (a) reuse the same `- [ ] [ready] …` position §5 uses (minimal grammar change, but now the "simple" layout is no longer simple — it has grown atelier metadata, which undercuts the whole "meet teams where they are" premise); (b) require an `#id` only for `[ready]` items and let `/plan-task` inject it on approval (keeps unplanned backlog clean, adds ids lazily); (c) a `github-project`-style out-of-band `Ready` field is not available for a plain-markdown operator project, so it is off the table for the `files` backend.
3. **Resolve the sovereignty boundary (the real blocker).** `PLAN.md` line 112 places all tracking-format handling in **crt**, not atelier; atelier only detects and delegates. Option B moves a *second operator-facing format* into atelier's own `task-discovery` parser. Decide explicitly whether that (i) violates the boundary, (ii) is acceptable because *selection* is atelier's job while *transformation* stays crt's, or (iii) belongs in crt as a `RoadmapBackend` variant that normalizes on read. This decision, more than the parsing mechanics, determines whether Option B is even in atelier's scope.
4. **If Option B is approved**, sketch the minimal implementation as a follow-up task: extend `task-discovery`'s selection to run the `[ready]` gate on the `High/Med/Low` layout for operator (non-atelier) projects; define the `#id` acquisition rule from step 2; update `/plan-task` to flip readiness and name `.plan/<id>.md` for that layout; and mirror the format-conformance signal in `atelier-setup-project`'s `detect_roadmap_format()` so onboarding stops *requiring* the §5 rewrite. Add hermetic tests alongside `hooks/tests/setup-project-roadmap-format-f74.test.sh` and `hooks/tests/workspace-status-roadmap-format.test.sh`.

The concrete work product of *this* task is the decision + a PLAN.md §5 edit recording it (either "Option B accepted, tracked as M7.1.F68-impl" or "Option A stands; revisit after §5 + `--format atelier` has mileage").

## Affected areas

- `PLAN.md` §5 (lines 261–338) — the `✅ agreed` spec that a "yes" on Option B would amend; the decision is recorded here either way. *(This task edits only PLAN.md; code paths below are for the follow-up implementation task, if Option B wins.)*
- `skills/task-discovery/SKILL.md` — *(follow-up, if approved)* the parser/selector: promote the line-117 `High/Med/Low` edge case from "atelier-only, gate-exempt" to a first-class operator path that applies the `[ready]` gate; define the `#id` / synthetic-id policy for `.plan` naming.
- `commands/plan-task.md` and `commands/next-task.md` — *(follow-up)* readiness flip + `.plan/<id>.md` naming and gate enforcement for the simple layout.
- `scripts/atelier-setup-project` (`detect_roadmap_format()`) and `scripts/atelier-workspace-status` (`roadmap_format()`) — *(follow-up)* stop signalling that a `High/Med/Low` operator roadmap *must* be adopted to §5.
- `hooks/tests/setup-project-roadmap-format-f74.test.sh`, `hooks/tests/workspace-status-roadmap-format.test.sh` — *(follow-up)* extend to cover the accepted-as-first-class case.
- **External coordination:** `claude-roadmap-tools` (crt) owns `/adopt-roadmap` and format transformation (`PLAN.md` line 112). Option B reduces crt's `--format atelier` from "required" to "optional", so the decision must be co-owned with crt and cannot land purely inside atelier.

## Acceptance criteria

- [ ] A decision record exists (in PLAN.md §5, and mirrored to this board item's plan/notes) that explicitly chooses **Option A (status quo)** or **Option B (native High/Med/Low for operator projects)**, with the rationale.
- [ ] The record enumerates what each §5-only field (`priority`, `#id`, `type`, `~estimate`, `[ready]`) buys and what is lost if it becomes optional — grounded in the real consumers named above, not hand-waved.
- [ ] The `#id` stability problem (synthetic-slug ids vs `.plan/<id>.md` and `blocked_by` linkage) is addressed head-on, with a concrete rule for where an id comes from under Option B.
- [ ] The sovereignty-boundary question (`task-discovery` parsing a second operator format vs crt owning transformation, per PLAN.md line 112) is resolved explicitly, and the resolution is co-signed with the crt owner rather than decided unilaterally in atelier.
- [ ] If Option B is chosen: a follow-up implementation task is filed (with the affected-areas list above) and PLAN.md §5 is amended to bless the format; if Option A stands: PLAN.md records the "revisit after §5 + `--format atelier` mileage" trigger so this does not silently die.
- [ ] No behavioural change ships from *this* task without the decision above being approved — this task is a decision, not an implementation.

## Risks / open questions

- **Changing a `✅ agreed` spec.** §5 is a decided, load-bearing contract; every downstream agent (`task-discovery`, `next-task`, `plan-task`, `task-decomposer`, size gate) assumes it. Option B widens the input surface of the most central parser in the system. Per CLAUDE.md, a `✅` item cannot be contradicted without an explicit PLAN.md flag — so this task's own output *is* that flag.
- **The `#id` instability is the real cost, not `type`/`estimate`.** Priority maps trivially and type/estimate degrade gracefully, but stable ids underpin plan-artifact naming, branch naming, and `blocked_by`. Option B has to solve id acquisition convincingly or it trades onboarding friction for a subtler correctness hazard (orphaned plans, broken dependencies on rename).
- **Sovereignty tension.** PLAN.md line 112 deliberately keeps format handling in crt. Native parsing in atelier could either be a clean "selection is atelier's job" refinement or an erosion of that boundary — reasonable people can disagree, so this needs an explicit, co-owned ruling.
- **"Simple layout" stops being simple.** The moment `[ready]` (and possibly `#id`) must appear in a `High/Med/Low` line to be claimable, the layout has absorbed atelier metadata — partially defeating the "meet teams where they are" motivation. Quantify how much friction Option B actually removes vs the one-time `--format atelier` conversion Option A already ships.
- **Timing.** The task body itself says to revisit only *once the §5 + `--format atelier` path has real mileage*. If that mileage does not yet exist, the honest decision may be "defer, keep Option A, set a concrete revisit trigger" rather than build Option B now. The plan is written to make "defer" a first-class, acceptable outcome.
- **This is atelier-dev (github-project backend).** There is no `ROADMAP.md` §5 block on this repo to decompose or edit for the task itself; the deliverable lands in `PLAN.md` and the board item. That is why this plan is flat.

## Decomposition

n/a — this is a single design-decision unit (deliverable = a documented A/B decision + PLAN.md note), not a multi-PR build. It is not decomposed: (1) atelier-dev uses the `github-project` backend, so there is no §5 ROADMAP block for `task-decomposer` to rewrite into an epic; (2) an Idea/design task's output is a decision, which does not split into independent sub-PRs. If Option B is approved, the *implementation* is filed as its own follow-up task and can be decomposed then.
