# Plan — #19c Command extensions + routing: /create-roadmap + /migrate-roadmap + activation

- **Status:** ready (approved — product lead)
- **Type / priority / estimate:** feat · P1 · ~TODO (suggest ~3h — near the budget; three files touched)
- **blocked_by:** #19b (activation must route to the Operations section #19b adds)

> **Deliverable repo:** PR lands in **`claude-roadmap-tools`**, editing `commands/create-roadmap.md`, `commands/migrate-roadmap.md`, and the activation/routing + direction-matrix parts of `skills/roadmap-tracking-flow/SKILL.md`. Markdown-only.

## Approach

Wire the new backend into setup, migration, and skill activation by extending the three places that currently branch on `files`/`linear`. Mirror the `linear` blocks closely so a `github-project` repo gets identical-shaped config and a consistent UX.

**1. `commands/create-roadmap.md`** — add a `github-project` backend path mirroring the `linear` path (steps 5b, the `.roadmap.json` template at lines 200–224, the arguments at lines 234–249):
   - Step 3 backend choice: add `github-project` alongside `files` / `linear`.
   - A new step **5c** mirroring **5b** (lines 38–63): ensure a **GitHub MCP** is registered (inspect MCP list for the GitHub-MCP host/name; if absent run the registration command — *endpoint TBD, the open question*; **never `gh`**), then list the user's Projects (the GitHub analogue of Linear's team-list — triggers OAuth on first call), pick a Project (owner + number, or accept `--project <owner/number-or-id>`), ask about the offline mirror, write `.roadmap.json` from a new **`github-project` template**, and (mirror on) create the indexed tracking files + `.gitignore` entries.
   - A new `### .roadmap.json — github-project backend` template subsection mirroring lines 200–224: `backend: "github-project"`, `offlineMirror`, and a `githubProject` block (`owner`, `projectNumber`/`projectId`, `historyWindow`, `stateMap` with the same camelCase keys + defaults `roadmap: ["Backlog","Todo"]`, `inProgress: ["In Progress"]`, `history: ["Done","Cancelled"]`). Same "ship full defaults, don't omit historyWindow" rule.
   - Arguments: `--backend github-project`; a `--project <owner/number-or-id>` flag (analogue of `--team`); `--mirror`/`--no-mirror` apply; reject `--layout` with `github-project` (always indexed, like Linear, line 239). Update the conflicting-flags rules + examples.
   - Step 7 reminder: point a `github-project` repo at the new `## Operations (GitHubProjectBackend)` section.

**2. `commands/migrate-roadmap.md`** — add `files (any layout) → github-project`:
   - The **Direction matrix** (lines 132–143): add rows — `files (single-file) → github-project` ✅, `files (indexed) → github-project` ✅, `github-project → files` ❌ (out of scope, like `linear → files`), `github-project → github-project` ❌ no-op, and `linear ↔ github-project` ❌ not-yet-implemented (cross-remote migration is #21 / M9.4 territory — keep it out of scope here and point at the matrix).
   - Step 2 (resolve target) + step 4 (branch): add a `--to github-project` branch mirroring step **5b** (lines 62–113): GitHub-MCP setup (reuse the create-roadmap 5c procedure by reference), parse all tasks from `ROADMAP.md`/`IN_PROGRESS.md`/`HISTORY.md`, show the migration plan (incl. the destructive local-delete preview when `mirror: false`), push each task to the Project as an item with the right Status + custom fields, write `backend: github-project` + `backendId: <item-id>` frontmatter back, and on full success write `.roadmap.json` as the atomic checkpoint. Carry the same partial-failure safety (stop, list created item ids + unpushed tasks, write nothing) and orphan-detection precondition (scan for `backendId` frontmatter from a failed run).
   - Reuse the `github-project` `.roadmap.json` template by reference to `/create-roadmap` (the doc's "one source of truth" rule, line 147).

**3. `skills/roadmap-tracking-flow/SKILL.md` — activation/routing only** (lines 10–63):
   - **Backend protocol** intro (lines 14–19): note `github-project` as a shipped third backend pointing at `## Operations (GitHubProjectBackend)`; drop it from the "future backends" line.
   - **When this skill applies** predicate 2 (lines 27–35): `.roadmap.json` with any backend already covers `github-project` — confirm the prose says "any backend (`files`, `linear`, or `github-project`)".
   - **Activation: detecting the active backend** (lines 46–63): step 1 add `backend: "github-project"` → use `## Operations (GitHubProjectBackend)`; extend the `offlineMirror` + mirror-refresh routing (steps 3–4) to apply to `github-project` too (the refresh *procedure* itself is #19d, but the routing that *selects* it is here — reference it so the edge to #19d is clean).
   - **Do NOT** touch the `## Operations (...)` bodies (that was #19b) or the `## Mirror auto-refresh on activation` procedure body (that is #19d) — only the routing that points at them.

## Affected areas

- `claude-roadmap-tools/commands/create-roadmap.md` — `github-project` backend path (new step 5c), `.roadmap.json` github-project template, arguments.
- `claude-roadmap-tools/commands/migrate-roadmap.md` — `--to github-project` branch, direction matrix rows, arguments.
- `claude-roadmap-tools/skills/roadmap-tracking-flow/SKILL.md` — activation/routing + backend-protocol intro + direction-matrix-adjacent prose **only** (no Operations bodies, no mirror-refresh procedure body).

## Acceptance criteria

- [ ] `/create-roadmap --backend github-project` writes `backend: github-project` into `.roadmap.json` (with a `githubProject` block: owner/project selector, `stateMap` defaults, `historyWindow`) and registers the GitHub MCP via OAuth — **never the `gh` CLI**.
- [ ] `/create-roadmap` rejects `--layout` with `github-project` (always indexed) and supports a `--project <owner/number-or-id>` flag plus `--mirror`/`--no-mirror`, with conflicting-flag errors mirroring the Linear rules.
- [ ] `/migrate-roadmap --to github-project` is added to the **direction matrix** (both `files` source layouts ✅; `github-project → files` and cross-remote directions ❌ with clear messages) and pushes every task to a Project, writing `backendId` frontmatter, with the same partial-failure + orphan-detection safety as the Linear path.
- [ ] The SKILL activation section detects `backend: github-project` and routes operations to the `## Operations (GitHubProjectBackend)` section; the offline-mirror routing extends to `github-project`.
- [ ] No edits to the `## Operations (...)` operation bodies or the `## Mirror auto-refresh on activation` procedure body (those belong to #19b / #19d).

## Risks / open questions

- **GitHub-MCP registration command is unconfirmed** (same open question as #19a). The registration step in 5c can't be finalized until the endpoint is known. The PR may have to land with a clearly-marked `TBD` placeholder for the exact `claude mcp add` URL if the endpoint is still unconfirmed at implementation time — flag for the product lead.
- **"List Projects" MCP capability.** The Linear path lists *teams*; the GitHub analogue lists *Projects* (org/user-owned). Whether the hosted GitHub MCP exposes a project-list tool is unverified — tied to the endpoint open question.
- **Budget pressure.** Three files, near the 200-line budget. If the migrate-roadmap branch pushes it over, the cleanest split would be to land create-roadmap + activation first and migrate-roadmap second — but the decomposer judged it fits as one prose slice (Linear analogues are ~37 + ~68 lines and much is by-reference). Watch the size gate; do not pad with duplicated template text — reference `/create-roadmap`'s template.
- **Cross-remote migration scope.** `linear → github-project` (and back) is explicitly *not* this task — it overlaps M9.4 (#21). Keep it out of the matrix as "not yet implemented" to avoid scope creep.

## Decomposition

Part of epic #19 (M9.2); siblings: #19a, #19b, #19d. This slice wires the #19b operations into the two commands and skill activation. #19d adds the offline-mirror procedure that this slice's routing points at.
