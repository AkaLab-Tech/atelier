# Plan — #8 `/validate` Python toolchain in `allowed-tools` frontmatter

- **Status:** ready (approved — product lead, batch approval via AskUserQuestion, 2026-07-07)
- **Type / priority / estimate:** chore · P2 · ~30min
- **blocked_by:** none

## Approach

`commands/validate.md`'s `allowed-tools` frontmatter grants the JS/TS toolchain explicitly (`Bash(eslint:*)`, `Bash(biome:*)`, `Bash(tsc:*)`, `Bash(vitest:*)`, `Bash(jest:*)`) plus a bare `Bash(pytest:*)` and `Bash(playwright:*)`, but the command body already detects Python projects and tries to run `ruff`, `mypy`/`pyright` — none of which are allow-listed. Add the three missing entries: `Bash(ruff:*)`, `Bash(mypy:*)`, `Bash(pyright:*)`. Purely additive frontmatter edit — no behavior change, just removes a permission-prompt stall under `claude -p`.

## Affected areas

- `commands/validate.md` — `allowed-tools` frontmatter only.

## Acceptance criteria

- [ ] `grep -E "Bash\(ruff|Bash\(mypy|Bash\(pyright" commands/validate.md` returns 3 matches.
- [ ] No other line in `commands/validate.md` changes.

## Risks / open questions

- None — this is a defensive, no-behavior-change frontmatter addition. Real validation (does `/validate` actually complete cleanly against a Python project) waits until atelier has a live Python project to test against, per the task's own "Trigger to revisit" note; that live check is out of scope for this PR.

## Decomposition

n/a — single-file, single-line-group change, well under the size budget.
