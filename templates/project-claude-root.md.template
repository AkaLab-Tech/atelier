# CLAUDE.md

<one-paragraph project pitch — what this project is, in 2-3 sentences>

## Stack

- **Language**: <e.g. TypeScript 5.x / Python 3.12 / Go 1.22 / TBD>
- **Framework**: <e.g. React 18 + Vite / Django 5 / fastapi / TBD>
- **Package manager**: <pnpm / npm / yarn / pip / cargo / TBD>
- **Test runner**: <vitest / jest / pytest / `go test` / TBD>
- **Linter / formatter**: <eslint + prettier / biome / ruff / TBD>

## Architecture

<short architectural summary — top-level directories + their purpose. For a new project: "TBD — no source files yet">

## Conventions

- **Test**: <how to invoke; e.g. `pnpm test` or `pytest -q` / TBD>
- **Lint**: <how to invoke / TBD>
- **Typecheck**: <how to invoke / TBD>
- **CI**: <one-line per workflow file, with trigger; e.g. "ci.yml on push to main / pr to main" / TBD>

## What this project is NOT

<explicit non-goals. Helps future agents avoid scope creep. TBD if nothing in the README or operator answer signals this.>

## Out of scope for AI agents

<things the operator wants to handle manually; AI agents should not touch. TBD if nothing explicit.>

---

<!--
This file is the project's authoritative context for Claude Code and atelier
agents. It is generated by `/atelier:setup-project` (step 9) the first time
the project is bootstrapped, then preserved on every subsequent re-run.

Edit it freely — the AI-driven draft is a starting point, not a contract.
The slash command refuses to overwrite this file once it exists.

For atelier-specific rules (push gates, retry budget, agent dispatch), see
`.claude/CLAUDE.md` — written from a separate template, kept in sync with
atelier's policies, not the project's.
-->
